spring:

  # Neural Networks
  ai:
    openai:
      base-url: ${OPENAI_BASE_URL}
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: ${OPENAI_MODEL}

# Logging
logging:
  level:
    org.springframework.ai: DEBUG
