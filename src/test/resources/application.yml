spring:

  # Database
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: test
    password: test
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: update

  # Neural Networks
  ai:
    openai:
      base-url: http://dummy.com
      api-key: dummy-key
      chat:
        options:
          model: dummy
